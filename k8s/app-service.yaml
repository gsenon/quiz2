apiVersion: v1
kind: Service
metadata:
  name: quiz-app-service
  namespace: quiz-system
  labels:
    app: quiz-app
spec:
  selector:
    app: quiz-app
  ports:
  - port: 80
    targetPort: 8000
  type: NodePort